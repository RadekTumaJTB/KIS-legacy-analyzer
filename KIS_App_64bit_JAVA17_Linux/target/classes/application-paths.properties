# =============================================================================
# KIS Banking Application - Platform-Independent Path Configuration
# =============================================================================
# This file replaces hardcoded Windows paths with configurable,
# platform-independent paths suitable for Linux deployment.
#
# Migration Date: 2025-12-05
# Critical Issue: 430+ hardcoded Windows paths across 84 files
# Key File: Constants.java (260+ references)
# =============================================================================

# -----------------------------------------------------------------------------
# Base Application Paths
# -----------------------------------------------------------------------------
# Primary base directory for KIS Banking application
# Windows default: D:\Konsolidace_JT\
# Linux default:   /opt/kis-banking/
kis.paths.base=/opt/kis-banking
kis.paths.root=${kis.paths.base}/Konsolidace_JT

# -----------------------------------------------------------------------------
# Data Directories
# -----------------------------------------------------------------------------
# Main data directory
kis.paths.data=${kis.paths.root}/data

# Excel files directory
kis.paths.xls=${kis.paths.root}/data

# CSV files directory
kis.paths.csv=${kis.paths.root}/csv

# Export directory
kis.paths.export=${kis.paths.root}/export

# Protocols/logs directory
kis.paths.protokol=${kis.paths.root}/protokoly

# Templates directory
kis.paths.sablony=${kis.paths.root}/sablony
kis.paths.sablony.cartesis=${kis.paths.sablony}/cartesis

# Document files directory
kis.paths.docfiles=${kis.paths.root}/docfiles

# EVI files directory
kis.paths.evifiles=${kis.paths.root}/evifiles

# Archive directory
kis.paths.archiv=${kis.paths.root}/archiv

# -----------------------------------------------------------------------------
# Temporary/Working Directories (.TEMP)
# -----------------------------------------------------------------------------
kis.paths.temp.base=${kis.paths.root}/.TEMP

# Administrative costs
kis.paths.temp.admin.naklady=${kis.paths.temp.base}/adminNaklady

# Balance detail
kis.paths.temp.bilance.detail=${kis.paths.temp.base}/bilanceDetail

# Budget crossing
kis.paths.temp.budget.prekroceni=${kis.paths.temp.base}/budPres

# Budget approval
kis.paths.temp.budget.schvalovani=${kis.paths.temp.base}/budSchval

# Document details
kis.paths.temp.doklady.detail=${kis.paths.temp.base}/dokladyDetail

# EVI OR
kis.paths.temp.evi.or=${kis.paths.temp.base}/eviOr

# Amount errors
kis.paths.temp.chyby.mustku=${kis.paths.temp.base}/chybyMustku

# KAP OR
kis.paths.temp.kap.or=${kis.paths.temp.base}/kap

# Consolidation administrator
kis.paths.temp.kons.spravce=${kis.paths.temp.base}/konsSpravce

# MPOP
kis.paths.temp.mpop=${kis.paths.temp.base}/mpop

# Amounts
kis.paths.temp.mustky=${kis.paths.temp.base}/mustky

# PB Clients
kis.paths.temp.pb.klienti=${kis.paths.temp.base}/pbKlientiA

# Project cards
kis.paths.temp.projekty.karta=${kis.paths.temp.base}/projektKarta

# Projects SL Developer
kis.paths.temp.projekty.sldev=${kis.paths.temp.base}/projektySLDev

# Project transactions
kis.paths.temp.projekty.transakce=${kis.paths.temp.base}/projektyTran

# Counter-party groups
kis.paths.temp.proti.group=${kis.paths.temp.base}/protiGroup

# SL Output
kis.paths.temp.sl=${kis.paths.temp.base}/slOutput

# SL Progress
kis.paths.temp.sl.postup=${kis.paths.temp.base}/slPostup

# Company transfers
kis.paths.temp.spol.predav=${kis.paths.temp.base}/spolPredav

# Unified accounts
kis.paths.temp.unif.ucty=${kis.paths.temp.base}/unifUcty

# Lock generation
kis.paths.temp.zamek.generovani=${kis.paths.temp.base}/zamekGen

# Counter-party changes
kis.paths.temp.zmeny.protistran=${kis.paths.temp.base}/zmenyProtistran

# Entrepreneur accounts
kis.paths.temp.podnikatel.ucty=${kis.paths.temp.base}/podnikatelUcty

# Lock protocol
kis.paths.temp.zamek.protokol=${kis.paths.temp.base}/zamekProtokol

# Change in ownership stake
kis.paths.temp.zmena.maj.ucast=${kis.paths.temp.base}/zmenaMU

# -----------------------------------------------------------------------------
# Data Directories (.DATA)
# -----------------------------------------------------------------------------
kis.paths.data.base=${kis.paths.root}/.DATA

# Budget standard
kis.paths.data.budget.std=${kis.paths.data.base}/budget

# Budget costs
kis.paths.data.budget.naklad=${kis.paths.data.base}/budgetMustekNaklad

# Budget project
kis.paths.data.budget.projekt=${kis.paths.data.base}/budgetProjekt

# Emissions
kis.paths.data.emise=${kis.paths.data.base}/emise

# IFRS changes
kis.paths.data.ifrs.zmeny=${kis.paths.data.base}/ifrszmeny

# Kamil documents
kis.paths.data.doklad.kamil=${kis.paths.data.base}/kamil

# Sub-consolidation
kis.paths.data.subkonsolidace=${kis.paths.data.base}/konsolidace

# Consolidation changes
kis.paths.data.kons.zmeny=${kis.paths.data.base}/konszmeny

# Property stakes
kis.paths.data.majetek=${kis.paths.data.base}/majetek

# Departments
kis.paths.data.odbory=${kis.paths.data.base}/odbory

# Position MU (prefix for generated names)
kis.paths.data.pozice.mu=${kis.paths.data.base}/pozice_

# Project documents
kis.paths.data.projekty.doklady=${kis.paths.data.base}/projektDoklad

# Projects
kis.paths.data.projekty=${kis.paths.data.base}/projekty

# Projects Cash Flow
kis.paths.data.projekty.cf=${kis.paths.data.base}/projektyCF

# Loans
kis.paths.data.uvery=${kis.paths.data.base}/projektyUvery

# Counter-party persons
kis.paths.data.proti.osoby=${kis.paths.data.base}/protiOsoby

# Companies
kis.paths.data.spolecnosti=${kis.paths.data.base}/spolecnosti

# Cartesis
kis.paths.data.cartesis=${kis.paths.data.base}/CARTESIS

# Position MU logs (prefix for generated names)
kis.paths.data.pozice.mu.logs=${kis.paths.data.base}/muProtokol/pozice_

# -----------------------------------------------------------------------------
# System Configuration Paths
# -----------------------------------------------------------------------------
# JAZN XML file path (Oracle security configuration)
# Windows default: C:\Oracle\product\j2ee10\j2ee\OC4J_app\application-deployments\kis\jazn-data.xml
# Linux default:   /opt/oracle/j2ee10/j2ee/OC4J_app/application-deployments/kis/jazn-data.xml
kis.paths.jazn.xml=/opt/oracle/j2ee10/j2ee/OC4J_app/application-deployments/kis/jazn-data.xml

# -----------------------------------------------------------------------------
# Backup Paths
# -----------------------------------------------------------------------------
# Backup base directory (typically on separate partition)
kis.paths.backup.base=/var/backup/kis-banking
kis.paths.backup.daily=${kis.paths.backup.base}/daily
kis.paths.backup.monthly=${kis.paths.backup.base}/monthly

# -----------------------------------------------------------------------------
# Temporary System Paths
# -----------------------------------------------------------------------------
# Java temp directory (uses system default)
kis.paths.temp.system=${java.io.tmpdir}/kis-banking

# -----------------------------------------------------------------------------
# Migration Notes
# -----------------------------------------------------------------------------
# 1. All paths use forward slashes (/) for platform independence
# 2. File.separator or Paths.get() should be used in code for path construction
# 3. Linux is case-sensitive: file.txt != File.txt
# 4. Ensure UTF-8 encoding for all file operations
# 5. Directory permissions must be set appropriately on Linux:
#    - Application user needs read/write access to all working directories
#    - Backup directories may need special permissions
# 6. Consider using symbolic links for legacy path compatibility
#
# Testing Required:
# - Verify all export/import operations on Linux
# - Test template loading from new paths
# - Validate backup operations
# - Check log file rotation and cleanup
# - Verify archive operations
# =============================================================================
